# tests/algorithms/

論文アルゴリズムの pytest を置くディレクトリ。

## 方針

- seed を固定して確率的テストを再現可能にする。
- テスト本文は `# Given` / `# When` / `# Then` の順に整理する。
- 論文、設定、Algorithm 番号が分かるディレクトリ・ファイル名にする。
- 論文手順の期待値と、簡略実装で意図的に未割当を露出する挙動を分けて確認する。
